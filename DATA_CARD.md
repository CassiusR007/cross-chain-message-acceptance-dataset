# MAAD v0.2.0 数据卡 / Data card

## 目的与单位

面向“基于跨链消息生命周期与验证语义的异常消息接受”研究，提供事件线索、可追溯主张与后续取证入口。当前单位是报告支持的事件；同一事件可以包含多条消息和多笔交易。保留的旧消息层是候选/观察层，不因关联攻击事件而自动变成异常真值。

核心包括源事实错误、事件发出者不可信、资产/金额不一致、证明无效或绑定错误、签名信任失效、防重失效及业务执行越权。真实通用消息被误作资产转移也是范围内问题。纯用户授权盗取、市场操纵、前端钓鱼不在核心；未利用披露、失败攻击和服务延迟单列。

## 采集与审阅

截止 2026-09-22。候选种子为冻结上游 203 条记录和已有 27 条研究线索。逐条初筛上游描述，读取主要候选的安全公司/项目方正文，记录字段冲突，并按事件定向搜索第二来源。全部输入 ID 都有映射；并非全部 203 条都有本轮直接报告核验。

已读取正文使用 `partial_text_read`，因为只提取相关章节，不宣称逐字通读全篇。搜索可见、正文失效的来源不能证明接受机制。首次机构查询中 `site.` 拼写不构成严格过滤，检索日志如实保留，并补做正确 `site:` 查询。第二来源搜索的 URL 按联合查询批次保存，不能假定每个命中对应批内每一个事件。

来源的 `evidence_origin_id` 指已识别的原始分析根源；转载应共用根源。`unknown` 不进入独立根源计数。单篇机构文章并不自动构成两方验证；统计和质量清单显示不足两个可确认根源的事件。

标注由本次助手研究流程整理，另有独立技术复审检查数据和工具；这不构成双人独立事实标注。未计算标注者一致性分数。未用攻击重放证实正文所有主张。

## 事件划分与冲突

一个桥不等于一个事件。THORChain 三次2021年事件、两次Poly事件分别记录；Aztec Connect 2026-06-14及06-15分别为事件，共用campaign，评估时应分在同组。IAVL VSA-2022-101披露不与BNB Token Hub攻击或Dragonberry披露混同。未解析组仍保留，不能再加到独立事件计数中。

日期优先保留正文原始语境；只有有明确时区的报告才转换UTC。THORChain Router II保留日期范围；Hypr和Wanchain与上游日期冲突已记录。Dedaub Poly页面日期元数据与正文冲突、Harmony签名门槛差异、Syscoin报告内部释放方向差异均保留。链列表可能是上游上下文，不一定等于已确认全部受害链；未完成部署级证据时 `deployment=null`。

## 适用与不适用

适合：事件分类研究、机制覆盖检查、消息级取证任务清单、定性威胁模型分析。

不适合直接用作：逐交易监督标签、检测器准确率基准、全行业损失/年度发生率统计、对个人或机构的归责结论。公开报告对严重、英语可检索及知名协议有选择偏差。2025年没有核心事件不表示该年没有攻击，而是本版核验覆盖不足。

本版不汇总美元损失：铸出面值、可兑现价值、流出额、回收额与不同时间价格不能直接相加。报告中的交易数也不等于本集实际采集样本数。多标签语义类别会重复计数。

## 缺失与后续取证

JSON null / CSV空字段表示缺失；数组用JSON编码。未知源消息不是正常，也不是已证实无源事件。交易入口 `report_link_only` 只证明报告给出了该标识，不证明receipt或角色已独立复验。需要补齐历史合约版本、source/destination配对、消息ID、nonce、最终性、验签集合与实际资产效果才能升级消息真值。

## 授权与维护

继承MIT许可及Justin Zhou署名。新增标注与工具遵循同一许可；外部报告没有被重新许可或全文镜像。通过GitHub issue提交纠错时，请给出事件ID、原始报告段落、链/交易入口及更正理由。修改既有ID应维护映射，不按日期重新编号。
